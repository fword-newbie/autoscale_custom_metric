apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: puyuan
spec:
  hosts:
    - puyuan
  http:
  - route:
    - destination:
        host: puyuan
        subset: v1
      weight: 50
    - destination:
        host: puyuan
        subset: v2
      weight: 50